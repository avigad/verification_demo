# verification_demo

To build, use
```
  lake exe cache get
```
to get the cached version of Mathlib, and
```
  lake build
```
to build the library and `demo` executable. Use
```
  build/bin/demo
```
to run it.
